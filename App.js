import React from 'react';
import { View, Text, StatusBar } from 'react-native';

export default function appCamera() {
 return (
   <View>
     <StatusBar hidden={true}/>
     <Text> App Camera</Text>
   </View>
  );
}